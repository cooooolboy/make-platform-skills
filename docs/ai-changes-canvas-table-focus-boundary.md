# canvas-table 焦点边界变更记录

## 2026-05-07

- 明确 canvas-table 可视化宿主不能被 `aria-hidden` 或 `inert` 隐藏，避免可聚焦 canvas 落在隐藏祖先内。
- 补充屏幕阅读器备用表格与可视化 canvas 宿主的职责边界：备用表格保持独立隐藏结构，可视化宿主保留非隐藏可访问名称。
- 收紧 canvas 焦点恢复规则：仅限 canvas-table 单元格编辑提交、取消或回滚后返回表格时使用。
- 明确当宿主弹窗、抽屉、对话框、浮层或表单打开时，应跳过或取消 canvas 延迟聚焦，避免抢占输入框焦点。
